services:
  redis:
    image: redis:7-alpine
    restart: unless-stopped
    # Shared QIDO cache for uvicorn workers. Not exposed to host; internal only.
    # Data is 100% derivable from Orthanc, no persistence needed — a cold Redis
    # just means the next /api/studies call repopulates from Orthanc.
    command: ["redis-server", "--save", "", "--appendonly", "no", "--maxmemory", "128mb", "--maxmemory-policy", "allkeys-lru"]
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 2s
      retries: 10

  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    volumes:
      - orthanc-pg:/var/lib/postgresql/data
    environment:
      POSTGRES_USER: orthanc
      POSTGRES_PASSWORD: "${POSTGRES_PASSWORD}"
      POSTGRES_DB: orthanc
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U orthanc -d orthanc"]
      interval: 5s
      timeout: 3s
      retries: 20
      start_period: 10s

  orthanc:
    image: orthancteam/orthanc
    restart: unless-stopped
    volumes:
      - orthanc-data:/var/lib/orthanc/db
      - ./orthanc/orthanc-docker.json:/etc/orthanc/orthanc.json:ro
      - ./orthanc/lua:/etc/orthanc/lua:ro
      - ./orthanc/python:/etc/orthanc/python:ro
      - minipacs-thumbs:/var/cache/minipacs-thumbs
      - ./orthanc/ohif-user-config.js:/etc/orthanc/ohif-user-config.js:ro
    environment:
      ORTHANC_USERNAME: "${ORTHANC_USERNAME:-orthanc}"
      ORTHANC_PASSWORD: "${ORTHANC_PASSWORD}"
      ORTHANC__REGISTERED_USERS: '{"${ORTHANC_USERNAME:-orthanc}":"${ORTHANC_PASSWORD}"}'
      DICOM_WEB_PLUGIN_ENABLED: "true"
      TRANSFERS_PLUGIN_ENABLED: "true"
      STONE_WEB_VIEWER_PLUGIN_ENABLED: "true"
      OHIF_PLUGIN_ENABLED: "true"
      POSTGRESQL_PLUGIN_ENABLED: "true"
      POSTGRESQL_HOST: postgres
      POSTGRESQL_PORT: "5432"
      POSTGRESQL_DATABASE: orthanc
      POSTGRESQL_USERNAME: orthanc
      POSTGRESQL_PASSWORD: "${POSTGRES_PASSWORD}"
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "48923:8042"  # Orthanc HTTP API (for Vite dev proxy)
      - "48924:4242"  # DICOM C-STORE/C-ECHO
    healthcheck:
      test: ["CMD-SHELL", "python3 -c \"import socket; s=socket.create_connection(('localhost',8042),2); s.close()\""]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s

  backend:
    build:
      context: ./backend
    restart: unless-stopped
    volumes:
      - minipacs-db:/app/data
      - ./scripts:/app/scripts:ro
      - minipacs-thumbs:/srv/thumbs:ro
    environment:
      SECRET_KEY: "${SECRET_KEY}"
      ORTHANC_URL: "http://orthanc:8042"
      ORTHANC_USERNAME: "${ORTHANC_USERNAME:-orthanc}"
      ORTHANC_PASSWORD: "${ORTHANC_PASSWORD}"
      REDIS_URL: "redis://redis:6379/0"
      DATABASE_URL: "sqlite+aiosqlite:///./data/minipacs.db"
      DATABASE_PATH: "/app/data/minipacs.db"
      CORS_ORIGINS: '["https://localhost:${HTTPS_PORT:-48921}","http://localhost:${HTTP_PORT:-48920}","http://localhost:48925"]'
      AUTO_LOGOUT_MINUTES: "${AUTO_LOGOUT_MINUTES:-15}"
      DEFAULT_SHARE_EXPIRY_DAYS: "${DEFAULT_SHARE_EXPIRY_DAYS:-30}"
    ports:
      - "48922:8000"  # Expose for Vite dev proxy
    depends_on:
      orthanc:
        condition: service_healthy
      redis:
        condition: service_healthy

  frontend:
    build:
      context: .
      dockerfile: frontend/Dockerfile
    restart: unless-stopped
    ports:
      - "${HTTP_PORT:-48920}:80"
      - "${HTTPS_PORT:-48921}:443"
    environment:
      ORTHANC_BASIC_AUTH: "${ORTHANC_BASIC_AUTH}"
      HTTPS_PORT: "${HTTPS_PORT:-48921}"
    depends_on:
      - backend
      - orthanc

volumes:
  orthanc-data:
  orthanc-pg:
  minipacs-db:
  minipacs-thumbs:
